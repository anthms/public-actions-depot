on: [push]

jobs:
  job-success:
    runs-on: ubuntu-latest
    steps:
      - run: echo "success!"
  job-skip:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "success!"
  job-fail-coe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        continue-on-error: true
        with:
          script: |
            core.setFailed('failure');
