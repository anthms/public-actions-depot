on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - uses: actions/checkout@v5
        with:
          repository: anthmmatic/private-party
