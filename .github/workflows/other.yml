on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://foo/bar@sha256:asdasdads
        with:
          args: sh -c '/a/b/c command > output.log'
