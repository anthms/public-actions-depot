on:
  push:

jobs:
  job1:
    if: true && !cancelled()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          counter=1
          until [ $counter -gt 50 ]
          do
            echo $counter
            ((counter++))
            sleep 20
          done
          echo All done
