on:
  #push:
  #pull_request:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - id: out
        run: |
          VAR=$(echo 'value')
          echo "OUTPUT: $GITHUB_OUTPUT"
          echo "vars=${VAR}" >> $GITHUB_OUTPUT
